<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>End_Date_after_Start_Date</fullName>
    <active>true</active>
    <description>End Date must be after Start Date, and Start Date must be set if End Date is populated.</description>
    <errorConditionFormula>AND(
    NOT(ISBLANK(End_Date__c)),
    OR(
        ISBLANK(Start_Date__c),
        End_Date__c &lt; Start_Date__c
    )
)</errorConditionFormula>
    <errorMessage>End Date must be after Start Date, and Start Date must be set if End Date is populated.</errorMessage>
</ValidationRule>
